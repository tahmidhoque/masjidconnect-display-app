/**
 * Prayer phase configuration.
 * Shared between usePrayerPhase and usePrayerTimes to avoid circular imports.
 */

/** How many minutes after jamaat to show the in-prayer screen */
export const IN_PRAYER_DURATION_MIN = 10;
